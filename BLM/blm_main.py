import sys
import scipy.ndimage
import nibabel as nib
import numpy as np
import subprocess
import warnings
import resource
from lib import blkXtX, blkXtY

def main(Y_files, X): #flag='spat'

    # This will be the function to call this as a python package
    # plan - call blm_setup
    #        loop through each batch using blm_batch
    #        call blm_concat

if __name__ == "__main__":
    main()
